import Header from "../components/Header";

type Props = {
}

export default function ProgramView(props: Props) {
    return (
        <>
            <Header currentUrl="program" />
            (todo)
        </>
    )
}
